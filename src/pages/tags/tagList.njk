---
layout: page.njk
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /tags/{{ tag | slugify }}/
eleventyComputed:
  title: "{{ tag }}"
---

<h1>Tagged “{{ tag }}”</h1>

{% for post in collections[tag] %}
<article class="article-item">
  {% if post.data.image %}
  <div class="article-item_cover">
    <figure>
      <a href="{{ post.url }}"><img
        src="../{{ post.data.image }}"
        alt="{{ post.data.title }}"
        width="{{ post.data.imageProp }}"
      /></a>
    </figure>
  </div>
  {% endif %}

  <section
    class="article-item_content {% if not(post.data.image) %}__shift{% endif %}"
  >
    <h2 class="article-item_title">
      <a href="{{ post.url }}" class="article-item_lk">
        {{ post.data.title }}
      </a>
    </h2>

    <div class="article-item__desc">
      {{ post.data.dsc }}
    </div>

    <span class="tag_i"
      ><span class="tag_lk"> {{ tag }} </span></span
    >
  </section>
</article>
{% endfor %}
